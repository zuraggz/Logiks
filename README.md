# Logiks CRUD

> **Note on the API key:** The API key in `lib/services/legos_service.dart` is included on purpose, so the project can be run and reviewed right away without any additional setup.

This repository contains the complete project. A more detailed README with setup instructions and project documentation will be added soon.
